# usenavii example

Minimal Flutter demo for [`usenavii`](https://pub.dev/packages/usenavii) —
seed field, size, mood, and a `NaviiGroup` stack.

## Run

From this directory:

```sh
flutter pub get
flutter run -d chrome    # web
flutter run -d macos     # macOS
```

Or from the package root:

```sh
cd packages/flutter/example
flutter run -d chrome
```

Requires the Flutter SDK (`>=3.24`). The app depends on the local package via
`path: ../`.

## Links

- [navii.dev](https://navii.dev)
- [GitHub](https://github.com/uxderrick/navii)
- [Package README](../README.md)
